import { Page } from \"../../core/base/page\";\n\nclass OrderSummaryPage extends Page {\n  private selectors = {\n    orderSummary: \"#order-summary\",\n    proceedToPaymentButton: \"#proceed-to-payment\",\n  };\n\n  async reviewOrderSummary(): Promise<void> {\n    await this.waitForElement(this.selectors.orderSummary);\n  }\n\n  async clickProceedToPayment(): Promise<void> {\n    await this.click(this.selectors.proceedToPaymentButton);\n  }\n\n  async isPaymentProcessingIncluded(): Promise<boolean> {\n    // Implement logic to verify if payment processing is part of order summary\n    // This is a placeholder implementation and should be replaced with actual logic\n    return false;\n  }\n}\n\nexport default new OrderSummaryPage();